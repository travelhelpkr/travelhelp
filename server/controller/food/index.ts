import { add, show, update, remove } from './cart';
import { get }from './menu';
import { addOrder, showAddress } from './order';

export const Food = {
  add,
  show,
  update,
  remove,
  get,
  addOrder,
  showAddress
}